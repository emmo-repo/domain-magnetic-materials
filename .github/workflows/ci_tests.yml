name: CI tests
on: [push]

jobs:
  test_generate_ontology_from_script:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Setup Pixi
      uses: prefix-dev/setup-pixi@v0.10.0
    - name: Test generated ontology
      run: pixi run test
